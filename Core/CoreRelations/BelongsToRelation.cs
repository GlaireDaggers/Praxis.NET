namespace Praxis.Core;

/// <summary>
/// Indicates that this entity belongs to another, and should be deleted along with it. Unlike ChildOf, it does not inherit its parent's transform
/// </summary>
public struct BelongsTo
{
}
